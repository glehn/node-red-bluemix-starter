applications:
- memory: 512M
  services:
  - Cloudant NoSQL DB-h5
  env:
    NODE_RED_STORAGE_NAME: Cloudant NoSQL DB-h5
declared-services:
  Cloudant NoSQL DB-h5:
    label: cloudantNoSQLDB
    plan:  Lite
